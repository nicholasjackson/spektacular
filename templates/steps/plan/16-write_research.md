## Step {{step}}: {{title}}

research.md has been written to `{{research_path}}`.

All three plan documents are on disk. Advance to the finished step:

{{config.command}} plan goto --data '{"step":"{{next_step}}"}'
